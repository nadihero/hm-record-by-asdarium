# Database MySQL (untuk Prisma 7 dengan MariaDB adapter)
DATABASE_HOST="localhost"
DATABASE_USER="root"
DATABASE_PASSWORD=""
DATABASE_NAME="hm_record"

# Legacy URL (untuk prisma migrate)
DATABASE_URL="mysql://root:@localhost:3306/hm_record"

# Upload directory
UPLOAD_DIR="./public/uploads"
